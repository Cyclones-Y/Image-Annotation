import LabelManager from '../components/annotation/LabelManager'

export default {
  title: 'Annotation/LabelManager',
  component: LabelManager
}

export const ManagementFlow = () => <LabelManager projectId="1" />
