services:
  # 前端服务
  image-annotation-frontend:
    build:
      context: ../Image-Annotation-frontend
      dockerfile: Dockerfile
    image: image-annotation-frontend:latest
    container_name: image-annotation-frontend-test
    ports:
      - "80:80"
    volumes:
      - ../Image-Annotation-frontend/bin/nginx.dockermy.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - image-annotation-backend-my
    networks:
      - image-annotation-network-test

  # 后端服务（MySQL版本）
  image-annotation-backend-my:
    build:
      context: ../Image-Annotation-backend
      dockerfile: Dockerfile.my
    image: image-annotation-backend-my:latest
    container_name: image-annotation-backend-my-test
    ports:
      - "9099:9099"
    depends_on:
      image-annotation-mysql:
        condition: service_healthy
      image-annotation-redis:
        condition: service_healthy
    networks:
      - image-annotation-network-test

  # MySQL服务
  image-annotation-mysql:
    image: mysql:8.0
    container_name: image-annotation-mysql-test
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ruoyi-fastapi
    ports:
      - "13307:3306"
    volumes:
      - ../Image-Annotation-backend/sql/ruoyi-fastapi.sql:/docker-entrypoint-initdb.d/01-ruoyi-fastapi.sql
      - ./disable_captcha.sql:/docker-entrypoint-initdb.d/02-disable-captcha.sql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci --skip-character-set-client-handshake=1
    networks:
      - image-annotation-network-test
    healthcheck:
      test: ["CMD", "bash", "-c", "mysqladmin ping -h localhost -u root -proot && mysql -h localhost -u root -proot -e 'SELECT 1'"]
      interval: 5s
      timeout: 10s
      retries: 30

  # Redis服务
  image-annotation-redis:
    image: redis:latest
    container_name: image-annotation-redis-test
    ports:
      - "16380:6379"
    networks:
      - image-annotation-network-test
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 10s
      retries: 30

# 网络配置
networks:
  image-annotation-network-test:
    name: image-annotation-network-test
    driver: bridge
